# Backend (Flask) Environment Variables
FLASK_ENV=development
POSTGRES_DB=avanan
POSTGRES_USER=avanan
POSTGRES_PASSWORD=avanan
POSTGRES_HOST=db
IP_API_URL=http://ip-api.com/json/

# Frontend (React) Environment Variables
# (Add any Vite/React env vars here, e.g., API base URL if needed)
# VITE_API_BASE_URL=http://localhost:8000

# PostgreSQL (if running outside Docker Compose)
# POSTGRES_DB=avanan
# POSTGRES_USER=avanan
# POSTGRES_PASSWORD=avanan